% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insertQuestion.R
\name{insertQuestion}
\alias{insertQuestion}
\title{Inserir uma nova pergunta em um XLSForm}
\usage{
insertQuestion(
  xlsformPath,
  newQuestion,
  afterName = NULL,
  position = NULL,
  outputPath = NULL
)
}
\arguments{
\item{xlsformPath}{Caminho para o arquivo XLSForm (.xlsx) existente.}

\item{newQuestion}{Uma lista nomeada ou um \code{data.frame} de uma linha contendo as colunas da nova pergunta (ex: \code{type}, \code{name}, \code{label}, etc.).}

\item{afterName}{Caractere. O nome (\code{name}) da pergunta após a qual a nova pergunta deve ser inserida. Se não for encontrado ou for \code{NULL}, a função tentará usar o parâmetro \code{position}.}

\item{position}{Inteiro. A posição da linha (índice baseado em 1, relativo aos dados da aba \code{survey}) onde a pergunta deve ser inserida. Se \code{afterName} e \code{position} forem \code{NULL}, a pergunta será adicionada ao final.}

\item{outputPath}{Caminho para salvar o arquivo modificado. Se for \code{NULL} (padrão), o arquivo original em \code{xlsformPath} será sobrescrito.}
}
\value{
Retorna o caminho do arquivo salvo, invisivelmente.
}
\description{
Esta função insere uma nova pergunta (linha) na aba \code{survey} de um arquivo XLSForm (Excel),
permitindo especificar a posição exata por meio do nome de uma pergunta existente ou por um índice numérico.
}
\examples{
\dontrun{
# Exemplo de nova pergunta como lista
nova_q <- list(
  type = "integer",
  name = "idade_anos",
  label = "Qual a sua idade em anos?",
  required = "yes"
)

# Inserir após a pergunta "nome_completo"
insertQuestion(
  xlsformPath = "cadastro.xlsx",
  newQuestion = nova_q,
  afterName = "nome_completo",
  outputPath = "cadastro_atualizado.xlsx"
)
}
}
